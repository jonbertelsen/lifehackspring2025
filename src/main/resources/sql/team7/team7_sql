-- This script was generated by the ERD tool in pgAdmin 4.
-- Please log an issue at https://github.com/pgadmin-org/pgadmin4/issues/new/choose if you find any bugs, including reproduction steps.
BEGIN;


CREATE TABLE IF NOT EXISTS public.team_7_admin
(
    team_7_username character varying(20) COLLATE pg_catalog."default" NOT NULL,
    team_7_password character varying(10) COLLATE pg_catalog."default" NOT NULL,
    CONSTRAINT team_7_admin_pkey PRIMARY KEY (team_7_username)
);

CREATE TABLE IF NOT EXISTS public.team_7_category
(
    category_id serial NOT NULL,
    category_names character varying(50) COLLATE pg_catalog."default" NOT NULL,
    CONSTRAINT team_7_category_pkey PRIMARY KEY (category_id)
);

CREATE TABLE IF NOT EXISTS public.team_7_joke
(
    joke_id integer NOT NULL DEFAULT nextval('joke_joke_id_seq'::regclass),
    joke_names character varying COLLATE pg_catalog."default" NOT NULL,
    category_id integer NOT NULL,
    CONSTRAINT joke_pkey PRIMARY KEY (joke_id)
);

CREATE TABLE IF NOT EXISTS public.team_7_subscribe
(
    subscribe_id integer NOT NULL DEFAULT nextval('subscribe_subscribe_id_seq'::regclass),
    subscribe_email character varying COLLATE pg_catalog."default" NOT NULL,
    CONSTRAINT subscribe_pkey PRIMARY KEY (subscribe_id)
);

ALTER TABLE IF EXISTS public.team_7_joke
    ADD CONSTRAINT category_id FOREIGN KEY (category_id)
    REFERENCES public.team_7_category (category_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;

END;