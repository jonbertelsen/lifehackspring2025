-- This script was generated by the ERD tool in pgAdmin 4.
-- Please log an issue at https://github.com/pgadmin-org/pgadmin4/issues/new/choose if you find any bugs, including reproduction steps.
BEGIN;


CREATE TABLE IF NOT EXISTS public.team11_category
(
    category_id integer NOT NULL,
    category_name character varying(150) COLLATE pg_catalog."default" NOT NULL,
    CONSTRAINT team11_category_pkey PRIMARY KEY (category_id)
);

CREATE TABLE IF NOT EXISTS public.team11_country
(
    country_id serial NOT NULL,
    country_name character varying(50) COLLATE pg_catalog."default" NOT NULL,
    country_moms double precision,
    CONSTRAINT team11_country_pkey PRIMARY KEY (country_id)
);

CREATE TABLE IF NOT EXISTS public.team11_tarrif_rate
(
    country_id integer NOT NULL,
    category_id integer NOT NULL,
    tariff_rate double precision,
    CONSTRAINT team11_tarrif_rate_pkey PRIMARY KEY (country_id, category_id)
);

ALTER TABLE IF EXISTS public.team11_tarrif_rate
    ADD FOREIGN KEY (country_id)
    REFERENCES public.team11_country (country_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION
    NOT VALID;


ALTER TABLE IF EXISTS public.team11_tarrif_rate
    ADD FOREIGN KEY (category_id)
    REFERENCES public.team11_category (category_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION
    NOT VALID;

END;