package app.persistence;

import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import java.sql.Connection;
import java.sql.SQLException;
import java.sql.Statement;

import static org.junit.jupiter.api.Assertions.*;

class Team6MapperTest {
    /*

    private final static String USER = "postgres";
    private final static String PASSWORD = "postgres";
    private final static String URL = "jdbc:postgresql://localhost:5432/%s?currentSchema=public";
    private final static String DB = "movie_guesser";
    private final static ConnectionPool connectionPool = ConnectionPool.getInstance(USER, PASSWORD, URL, DB);
    private final static Team6Mapper movieMapper = new Team6Mapper();

    @Test
    void dummyTest() {
        assertTrue(true);
    }

    @Test
    void getTop100MoviesTest() {
        int expectedSize = 100;
        int actualSize = Team6Mapper.getTop100Movies(connectionPool).size();

        assertEquals(expectedSize, actualSize);
    }
    */

}